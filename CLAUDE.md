# CLAUDE.md

## Git operations

- 対話セッション（ユーザーがリアルタイムでやり取りしているセッション）では、ユーザーがチャット上で明示的に「pushして」「マージして」と依頼した場合、mainブランチへのpushおよびPRのマージを確認なしで実行してよい。都度の確認は不要。
- ただし、バックグラウンドジョブ（`/loop`やスケジュール実行などユーザーがその場にいない自動実行）では、mainへのpush・force-push・PRマージは行わない。作業はブランチにコミット・pushし、Draft PRを作成した上でユーザーに引き継ぐこと。これはハーネス側の制約であり、CLAUDE.mdの記述では上書きできない。
