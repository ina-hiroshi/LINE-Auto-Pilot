-- PostgRESTのスキーマキャッシュをリロード
-- これにより、setup_service_ordersテーブルのリレーションが認識される可能性があります
-- 
-- 注意: user_idは既にauth.users(id)を参照していますが、
-- profiles.id = auth.users.id という関係があるため、
-- PostgRESTが profiles:user_id リレーションを認識できる可能性があります
--
-- もしリレーションが認識されない場合は、現在の実装（別々のクエリ）を使用してください。
-- 現在の実装でも安全性は保証されています：
-- 1. データ整合性: 外部キー制約により保護されています
-- 2. セキュリティ: RLSポリシーが各テーブルに適用されます
-- 3. パフォーマンス: バッチクエリ（in句）により最適化されています

NOTIFY pgrst, 'reload schema';
